apiVersion: v1
kind: service
metadata:
  name: lms-service
spec:
  type: NodePort
  selector:
    app: lms-app
  ports:
      - protocol: TCP 
        port: 80
        targetPort: 80
        nodePort: 30007 